# Changes — 12-04-26-i (Phase 4c Session 3 — Macro Snapshots)

## Phase 4c-S4 — Macro Buffer in Snapshots

### 254. KeymapSnapshot schema bump and MacroBuffer property

**Modified:** `src/SvalboardLayerViz.Core/History/KeymapSnapshot.cs`

- Bumped `CurrentSchemaVersion` from 2 → 3
- Added `public byte[]? MacroBuffer { get; init; }` — null for pre-v3 snapshots or no-macro devices
- System.Text.Json serializes `byte[]` as Base64 automatically; v2 snapshots deserialize with null — no migration needed

### 255. CaptureAsync gains macroBuffer parameter

**Modified:** `src/SvalboardLayerViz.Core/History/ISnapshotService.cs`, `src/SvalboardLayerViz.Core/History/SnapshotService.cs`

- Added `byte[]? macroBuffer = null` param between `keymap` and `userLabel`
- SnapshotService defensively clones the buffer on capture

### 256. MainWindowViewModel macro buffer helpers and capture site updates

**Modified:** `src/SvalboardLayerViz.App/ViewModels/MainWindowViewModel.cs`

- `GetDeviceMacroBuffer()` private helper — encodes `KeyboardConfig.Macros` via `MacroCodec`
- `GetDeviceMacroBufferForDiff()` public accessor — null-safe for diff dialog chain
- All 4 capture sites updated: PreSave passes baseline macro buffer, PostSave/FirstConnect/Manual pass current device macro buffer

### 257. Macro buffer threaded through history → diff dialog chain

**Modified:** `src/SvalboardLayerViz.App/ViewModels/HistoryWindowViewModel.cs`, `src/SvalboardLayerViz.App/App.axaml.cs`

- `HistoryWindowViewModel` constructor gains `byte[]? currentDeviceMacroBuffer` param, passes it through to `SnapshotDiffDialogViewModel` in `OpenDiffAsync`
- `App.axaml.cs` passes `viewModel.GetDeviceMacroBufferForDiff()` at construction

### 258. Macro-aware snapshot restore

**Modified:** `src/SvalboardLayerViz.App/ViewModels/SnapshotDiffDialogViewModel.cs`

- Constructor gains `byte[]? currentDeviceMacroBuffer` param
- `RestoreAsync` creates `KeymapEditSession` with device macro buffer, then applies `SetMacroBufferOp` if snapshot has macros and they differ from device
- Identical buffers or null on either side → no macro op (safe no-op)

### 259. FakeSnapshotService updated for new interface

**Modified:** `src/SvalboardLayerViz.Tests/Protocol/FakeSnapshotService.cs`

- `CaptureAsync` signature matches new interface with `macroBuffer` param
- Defensively clones and stores macro buffer on captured snapshot

### 260. Snapshot macro tests

**Modified:** `src/SvalboardLayerViz.Tests/History/SnapshotServiceTests.cs`

- `CaptureAsync_WithMacroBuffer_RoundTrips` — v3 capture + load round-trip, Base64 serialization
- `CaptureAsync_NullMacroBuffer_StillV3` — null macros still produce v3 schema
- `LoadAsync_V2Snapshot_MacroBufferIsNull` — hand-crafted v2 JSON loads with null MacroBuffer
- `CaptureAsync_MacroBuffer_IsDefensivelyCopied` — mutating source array after capture doesn't affect persisted data

### 261. Diff dialog macro restore tests

**Modified:** `src/SvalboardLayerViz.Tests/ViewModels/SnapshotDiffDialogViewModelTests.cs`

- `RestoreCommand_WithMacroBuffer_RestoresMacros` — snapshot macros differ from device → `HasPendingMacroChanges` true
- `RestoreCommand_NullMacroBuffers_NoMacroChanges` — null on both sides → no macro changes
- `RestoreCommand_IdenticalMacroBuffers_NoMacroChanges` — same buffer → no macro op applied

## Test Summary

- 855 tests (was 848 after S3), all passing, 0 warnings
