# Changes — 12-04-26-j (Phase 4c Session 4 — Macro Editor Dialog + Bug Fixes)

## Phase 4c-S5 — Macro Editor Dialog UI

### 262. MacroActionViewModel

**New file:** `src/SvalboardLayerViz.App/ViewModels/MacroActionViewModel.cs`

- Wraps one `MacroAction` with display + inline editing capability
- `TypeLabel` ("Tap"/"Hold"/"Release"/"Delay"/"Text"), `ValueLabel` (keycode name, delay ms, or text)
- `DelayMs` / `TextContent` settable properties for inline editing — fire `Changed` callback on mutation
- `IsKey` property for gating edit-key command
- `ReplaceAction()` swaps underlying action and refreshes all display properties

### 263. MacroSlotViewModel

**New file:** `src/SvalboardLayerViz.App/ViewModels/MacroSlotViewModel.cs`

- Per-slot VM: `Index`, `DisplayIndex` ("M0", "M1", ...), mutable `ObservableCollection<MacroAction> Actions`
- `Preview` auto-computed: empty → "(empty)", text → show text, keys → type+code summaries, truncated at ~40 chars
- `RefreshPreview()` callable after external mutations (move/edit actions)

### 264. MacroEditorViewModel

**New file:** `src/SvalboardLayerViz.App/ViewModels/MacroEditorViewModel.cs`

- Two-panel dialog VM: slot list (left), action list (right), buffer usage bar (bottom)
- Commands: `AddTapAction`, `AddDownAction`, `AddUpAction`, `AddDelayAction`, `AddTextAction`, `RemoveAction`, `MoveActionUp`, `MoveActionDown`, `EditKeyAction`, `Save`, `Cancel`
- `RequestKeyPick` callback for key picker modal — validates returned code ≤ 0xFF (HID byte range only)
- `EditKeyAction` opens picker for existing tap/down/up, replaces action in-place
- `RecalculateBufferSize()` after every mutation via `MacroCodec.ComputeEncodedSize()`
- `CanSave` gated on `IsEditMode && !IsOverCapacity`
- `Save` encodes all slots via `MacroCodec.Encode()`, calls `applyMacroEdit` callback

### 265. MacroEditorDialog view

**New file:** `src/SvalboardLayerViz.App/Views/MacroEditorDialog.axaml`, `MacroEditorDialog.axaml.cs`

- 620×500 window, two-column Grid: slot ListBox (left), action ListBox + button row (right)
- Bottom dock: ProgressBar bound to `UsagePercent` (red when over capacity), bytes label, Save/Cancel
- Action DataTemplate: type badge, value label (clickable Button for key actions via `#ActionList.DataContext.EditKeyActionCommand`), inline TextBox for delay/text
- Add button with Flyout menu (Tap/Down/Up/Delay/Text), Remove/MoveUp/MoveDown buttons
- All mutation buttons gated on `IsEditMode`

### 266. MainWindowViewModel: OpenMacros command + MacrosLoaded event

**Modified:** `src/SvalboardLayerViz.App/ViewModels/MainWindowViewModel.cs`

- `OpenMacrosRequested` callback property + `[RelayCommand] OpenMacros()`
- `MacrosLoaded` event fired when deferred macro load completes

### 267. App.axaml.cs: macro dialog wiring

**Modified:** `src/SvalboardLayerViz.App/App.axaml.cs`

- Constructs `MacroEditorViewModel` from `KeyboardConfig.Macros`, wires `Saved`/`Cancelled` to close dialog
- Key picker modal: `pickerDialog.ShowDialog(macroEditorDialog)` — modal over macro dialog, not mainWindow
- Subscribes to `MacrosLoaded` event → calls `UpdateFromBuffer()` on open dialog VM
- Unsubscribes on dialog close to prevent leaks

### 268. MainWindow.axaml: Macros button

**Modified:** `src/SvalboardLayerViz.App/Views/MainWindow.axaml`

- Macros button (pencil icon) between History and Refresh, visible when connected

### 269. Localization strings (en + nl)

**Modified:** `src/SvalboardLayerViz.App/Resources/Strings.resx`, `Strings.nl.resx`

- 17 keys: `Tooltip_Macros`, `MacroEditor_Title`, `MacroEditor_Slots`, `MacroEditor_Actions`, `MacroEditor_AddTap/Down/Up/Delay/Text`, `MacroEditor_Remove/MoveUp/MoveDown`, `MacroEditor_BufferUsage/Empty/OverCapacity/ReadOnly`, `Edit_Saving`

### 270. MacroEditorViewModel tests

**New file:** `src/SvalboardLayerViz.Tests/ViewModels/MacroEditorViewModelTests.cs`

- 27 tests: load from buffer, null buffer, select slot, add tap/delay/text, remove, move up/down, save encodes correctly, save blocked when over capacity, cancel no-op, read-only blocks mutations, edit key action changes keycode, edit key on non-key no-op, edit key in read-only no-op, UpdateFromBuffer populates/replaces, full-cycle integration (edit→save→reopen data survives)

### 271. MacroSlotViewModel tests

**New file:** `src/SvalboardLayerViz.Tests/ViewModels/MacroSlotViewModelTests.cs`

- 11 tests: preview empty/text/key/mixed/truncation, display index format, action collection sync

## Post-S5 Bug Fixes (device testing)

### 272. Macro save not reflected in UI + DirtyCount stuck at 1

**Modified:** `src/SvalboardLayerViz.App/ViewModels/MainWindowViewModel.cs`

- `ReloadKeymapFromDeviceAsync` now refreshes `KeyboardConfig.Macros` after save
- Uses `knownMacroBuffer` (the buffer just written) to skip slow USB re-read
- `KeyboardConfig = KeyboardConfig with { Macros = freshMacros }` updates model

### 273. Deferred macro loading — fast startup

**Modified:** `src/SvalboardLayerViz.Core/Keymap/KeymapLoader.cs`, `src/SvalboardLayerViz.App/ViewModels/MainWindowViewModel.cs`

- Removed macro buffer reading from `KeymapLoader.Load()` (was blocking connect for ~5s)
- Added `LoadMacrosAsync()` fire-and-forget after UI is up
- `SetMacroBaseline()` patches edit session when macros arrive after session creation

### 274. KeymapEditSession.SetMacroBaseline for late injection

**Modified:** `src/SvalboardLayerViz.Core/Keymap/KeymapEditSession.cs`

- Changed `_baselineMacroBuffer` from `readonly` to mutable
- `SetMacroBaseline(byte[])` — clones and sets both baseline + current macro buffer, no-op if already set
- Fixes race: edit session created before deferred macro load completes

### 275. MacroEditorViewModel.UpdateFromBuffer for live UI update

**Modified:** `src/SvalboardLayerViz.App/ViewModels/MacroEditorViewModel.cs`

- `UpdateFromBuffer(MacroBuffer)` replaces all slot data when deferred load completes while dialog is open
- Clears selection, rebuilds slots, recalculates buffer size, notifies capacity/usage properties

### 276. MacroCodec.Encode overload + ComputeEncodedSize

**Modified:** `src/SvalboardLayerViz.Core/Macros/MacroCodec.cs`

- `Encode(MacroBuffer)` convenience overload — encodes from MacroBuffer directly
- `ComputeEncodedSize(List<Macro>)` — calculates encoded size without allocating full buffer

### 277. MacroCodec tests for new methods

**Modified:** `src/SvalboardLayerViz.Tests/Macros/MacroCodecTests.cs`

- Tests for `ComputeEncodedSize`, `Encode(MacroBuffer)` overload, round-trip consistency

### 278. Skip macro reload when no macros changed

**Modified:** `src/SvalboardLayerViz.App/ViewModels/MainWindowViewModel.cs`

- `ReloadKeymapFromDeviceAsync` gains `skipMacroReload` parameter
- Save path passes `skipMacroReload: true` when no `MacroBufferWrite` in write list
- Eliminates slow chunked USB re-read (~26 chunks) on non-macro saves

### 279. Save button progress indicator

**Modified:** `src/SvalboardLayerViz.App/Views/MainWindow.axaml`, `Strings.resx`, `Strings.nl.resx`

- Save button swaps text to "Saving…" / "Opslaan…" while `IsSaving` is true
- Uses Panel with two TextBlocks toggled by `IsVisible` binding

## Test Summary

- 905 tests, all passing, 0 warnings
