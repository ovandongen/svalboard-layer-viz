<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="SvalboardLayerViz.App.App"
             RequestedThemeVariant="Dark">

    <Application.Styles>
        <FluentTheme />
    </Application.Styles>

    <!-- System tray icon -->
    <TrayIcon.Icons>
        <TrayIcons>
            <TrayIcon ToolTipText="Svalboard Layer Viz">
                <TrayIcon.Menu>
                    <NativeMenu>
                        <NativeMenuItem Header="Show Layers" Command="{Binding ShowCommand}" />
                        <NativeMenuItemSeparator />
                        <NativeMenuItem Header="Refresh from Device" Command="{Binding RefreshCommand}" />
                        <NativeMenuItemSeparator />
                        <NativeMenuItem Header="Quit" Command="{Binding QuitCommand}" />
                    </NativeMenu>
                </TrayIcon.Menu>
            </TrayIcon>
        </TrayIcons>
    </TrayIcon.Icons>

</Application>
